# NevUp Track 1 — environment variables
# Copy to .env for local docker-compose runs. The compose file reads .env automatically.

# ─── JWT (canonical kickoff secret — do NOT change for grading) ───
JWT_SECRET=97791d4db2aa5f689c3cc39356ce35762f0a73aa70923039d8ef72a2840a1b02

# ─── Postgres ───
POSTGRES_USER=nevup
POSTGRES_PASSWORD=nevup
POSTGRES_DB=nevup
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgres://nevup:nevup@postgres:5432/nevup

# ─── Redis ───
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URL=redis://redis:6379

# ─── API ───
API_PORT=3000
LOG_LEVEL=info
NODE_ENV=production

# ─── Web (admin UI) ───
WEB_PORT=8080
VITE_API_BASE_URL=http://localhost:3000
