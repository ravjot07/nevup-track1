app = "nevup-worker"
primary_region = "iad"

[build]
  dockerfile = "worker/Dockerfile"

[env]
  NODE_ENV = "production"
  LOG_LEVEL = "info"
  PG_POOL_MAX = "8"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
